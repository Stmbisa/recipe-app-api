version: "3.9"

services:
  app:
    build:
      context: .
      args:
        - DEV=true
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app
    command: >
      sh -c "python manage.py runserver 0.0.0.0:8000"

  # flake8:
  #   build:
  #     context: .
  #     args:
  #       - DEV=true
  #   volumes:
  #     - ./app:/app
  #   command: >
  #     sh -c "/py/bin/flake8 /app"
